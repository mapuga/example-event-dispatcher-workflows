name: Deploy Ambiente X
on:
  repository_dispatch:
    types: [run-jira-deploy]
jobs:
  deploy-qa:
    runs-on: ubuntu-20.04
    steps:
      - name: Obteniendo datos
        env:
            RAMA: ${GITHUB_REF_NAME}
        run: |
          if [[ "$RAMA" == "master" ]];
          then
            echo running on branch ${GITHUB_REF_NAME}
          else
            echo No esta en la rama
          fi

